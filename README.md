Constructor Injection in Spring Boot

This project demonstrates the use of constructor injection in Spring Boot, a type of dependency injection where dependencies are injected through a class constructor, promoting immutability and explicit dependencies.
